namespace Activ.PuP{
public static class RemotePackagePrep{

    // TODO for remote packages normally we only process them
    // if they are not in the manifest
    public static string Process(string src, Dependency dep){
        return src;
    }

}}
